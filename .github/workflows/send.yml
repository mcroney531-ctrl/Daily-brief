name: Send Daily Briefing

on:
  schedule:
    # 11:30 UTC = 6:30am EST. GitHub Actions cron is fixed UTC — no DST
    # awareness — so during Eastern Daylight Time (roughly mid-March to
    # early November) this lands at 7:30am local instead of 6:30. Bump to
    # "30 10 * * *" for those months if the hour drift bugs you, or leave
    # it — flagging it here so it's a known tradeoff, not a surprise.
    - cron: "30 11 * * *"
  workflow_dispatch: {} # lets you trigger a real send manually from the Actions tab, for testing

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      # Temporary — diagnosing a persistent Gmail 535 BadCredentials failure.
      # Prints only derived facts (lengths, yes/no checks), never the values
      # themselves, so nothing sensitive lands in the logs. Remove once the
      # send works.
      - name: Check SMTP secret shape
        env:
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_SECURE: ${{ secrets.SMTP_SECURE }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
        run: |
          echo "SMTP_USER  set: $([ -n "$SMTP_USER" ] && echo yes || echo no), length: ${#SMTP_USER}, domain: ${SMTP_USER##*@}"
          echo "SMTP_PASS  set: $([ -n "$SMTP_PASS" ] && echo yes || echo no), length: ${#SMTP_PASS} (a Google app password is 16)"
          case "$SMTP_PASS" in *" "*) echo "SMTP_PASS  contains spaces: yes";; *) echo "SMTP_PASS  contains spaces: no";; esac
          case "$SMTP_PASS" in *[!a-zA-Z0-9\ ]*) echo "SMTP_PASS  has non-alphanumeric chars: yes (unexpected)";; *) echo "SMTP_PASS  has non-alphanumeric chars: no";; esac
          echo "SMTP_HOST  length: ${#SMTP_HOST} (smtp.gmail.com is 14)"
          echo "SMTP_PORT  length: ${#SMTP_PORT} (587 is 3), is 587: $([ "$SMTP_PORT" = "587" ] && echo yes || echo no)"
          echo "SMTP_SECURE is exactly 'false': $([ "$SMTP_SECURE" = "false" ] && echo yes || echo no)"

      - run: npx tsx src/index.ts
        env:
          # ProjDash and quicksum-remote are open, unauthenticated MCP
          # endpoints (verified directly against their source, both the
          # Netlify and Railway deployments) — no token needed, safe to
          # keep as plain values here rather than repo secrets.
          PROJDASH_MCP_URL: https://project-tracker-mcp-production.up.railway.app/mcp
          QUICKSUM_MCP_URL: https://quicksum-mcp-production.up.railway.app/mcp

          # LinkHoard's auth status wasn't confirmed the same way (couldn't
          # verify API_TOKEN is set on Railway) — URL kept plain since a URL
          # alone isn't sensitive, but the token stays a secret in case one's
          # configured. Leave LINKHOARD_API_TOKEN unset in repo secrets if
          # there isn't one.
          LINKHOARD_API_URL: https://web-production-1b9bf.up.railway.app
          LINKHOARD_API_TOKEN: ${{ secrets.LINKHOARD_API_TOKEN }}

          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          SMTP_SECURE: ${{ secrets.SMTP_SECURE }}

          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_TO: mcroney531@gmail.com
